# 01-ЛОКАЛЬНЫЙ ЗАПУСК И СБОРКА

Статус: 📋 Ожидает
Приоритет: 🔥 Критично

---

## История правок

| Дата | Время | Что сделано |
|------|-------|-------------|
| 2025-12-18 | 00:00 | Создан каркас документа |

---

## Суть

Минимальные команды для разработки и проверки.

---

## Требования

- Rust stable

### Windows (toolchain MSVC)

- Установлен **Visual Studio Build Tools** с нагрузкой **Desktop development with C++**
- Установлен **Windows 10/11 SDK** (в т.ч. UCRT)
- Рекомендуемый запуск команд из **Developer PowerShell / Developer Command Prompt**
    для VS (чтобы подхватывались `cl.exe`, `link.exe`, `LIB`, `INCLUDE`)

---

## Команды

- Сборка debug: `cargo build`
- Сборка release: `cargo build --release`
- Запуск: `cargo run`
- Тесты: `cargo test`

---

## Примечания

- Если проект использует feature-флаги для платформенных бэкендов,
  перечислить их здесь.

---

## Troubleshooting (Windows)

### Ошибка: `LNK1104: cannot open file "msvcrt.lib"`

Это почти всегда означает, что **не установлены MSVC C++ библиотеки**
(папка `...\VC\Tools\MSVC\<version>\lib\x64` отсутствует или пуста).

Проверка наличия (PowerShell):

- `Test-Path "C:\Program Files (x86)\Microsoft Visual Studio\2019\BuildTools\VC\Tools\MSVC"`
- `Get-ChildItem "C:\Program Files (x86)\Microsoft Visual Studio\2019\BuildTools\VC\Tools\MSVC" -Directory | Select-Object -First 5 Name`
- `Get-ChildItem "C:\Program Files (x86)\Microsoft Visual Studio\2019\BuildTools\VC\Tools\MSVC" -Recurse -Filter msvcrt.lib -ErrorAction SilentlyContinue | Select-Object -First 5 -ExpandProperty FullName`

Если `msvcrt.lib` не находится:

- Visual Studio Installer → Modify → Workloads → **Desktop development with C++**
- Individual components → убедиться, что установлен компонент уровня
    **MSVC v142 x64/x86 build tools** (для VS 2019)

После доустановки — открыть Developer PowerShell и повторить `cargo build`.

### Предупреждения: `LNK4272: ... um\x86 ... конфликтует ... x64` + `LNK1120`

Это означает, что в окружении активированы **x86 пути библиотек**, а сборка идёт
для **x64**.

Что сделать:

- Запускать сборку из **x64 Native Tools Command Prompt for VS 2019**
    (или Developer Prompt, но с `-arch=amd64 -host_arch=amd64`).
- В уже открытом окне можно переинициализировать окружение:
    - `set VSINSTALLDIR=`
    - `call "C:\Program Files (x86)\Microsoft Visual Studio\2019\BuildTools\Common7\Tools\VsDevCmd.bat" -arch=amd64 -host_arch=amd64`
- Проверка: в `LIB` должны быть пути `...\Windows Kits\10\Lib\...\um\x64` и
    `...\ucrt\x64`, а не `x86`.

---

## Связано

- [02-СТРУКТУРА ПРОЕКТА workspace crates/01-АРХИТЕКТУРА workspace crates.md](../02-%D0%A1%D0%A2%D0%A0%D0%A3%D0%9A%D0%A2%D0%A3%D0%A0%D0%90%20%D0%9F%D0%A0%D0%9E%D0%95%D0%9A%D0%A2%D0%90%20workspace%20crates/01-%D0%90%D0%A0%D0%A5%D0%98%D0%A2%D0%95%D0%9A%D0%A2%D0%A3%D0%A0%D0%90%20workspace%20crates.md)

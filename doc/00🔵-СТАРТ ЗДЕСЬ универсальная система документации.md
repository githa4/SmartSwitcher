# 00-СТАРТ ЗДЕСЬ универсальная система документации

Статус: ✅ Готово  
Приоритет: 🔥 Критично

---

## История правок

| Дата | Время | Что сделано |
|------|-------|-------------|
| 17.12.2025 | 06:30 | Создан универсальный стандарт для старта новых проектов |
| 17.12.2025 | 07:15 | Объединены правила/шаблоны/схема в один документ |

---

## Суть

Это **единый источник правды** по документации проекта:

- какую структуру папок держим;
- как называем файлы/папки;
- какие шаблоны используем;
- как обновляем CHANGELOG/ROADMAP;
- что считается “утверждено/заморожено”.

Цель: чтобы владельцу проекта было удобно, а AI/ассистент работал быстрее и точнее.

---

## Главные принципы

1) **Один документ = одна тема** (не делайте “свалку”).
2) **Каноническое место — одно** (если правило продублировано, оно начнёт расходиться).
3) **Статусы и приоритеты — внутри документов**, а не в названии.
4) **Минимум текста, максимум чеклистов** (коротко, но проверяемо).

---

## Где живёт документация

Рекомендуемая папка: `docs/`.

Допускается (как в этом проекте): `doc/`.

Правило: выбрали один вариант — используем его везде.

---

## Роли главных файлов (чтобы не было дублей)

### README.md (в корне проекта)

Операционный документ:
- как запустить;
- как проверить;
- где конфиг/ENV;
- типичные команды;
- минимальный контекст “что это за проект”.

### 01🔵-README.md (в папке документации)

“Карта доков”:
- ссылки на ключевые документы;
- порядок чтения;
- где что лежит.

### CHANGELOG.md (в корне проекта)

Единый источник правды по изменениям:
- `[Unreleased]` всегда сверху;
- записи делаются на значимые изменения (не на правку текста);
- тема для коммита берётся из CHANGELOG.

---

## Минимальная структура (универсальная)

Минимально-полная структура для большинства проектов:

```
<project>/
├── README.md
├── CHANGELOG.md
└── docs/   (или doc/)
    ├── 00🔵-СТАРТ ЗДЕСЬ универсальная система документации.md
    ├── 01🔵-README.md
    ├── 03🔵-ROADMAP.md
    │
    ├── 01-ОБЩАЯ ИНФОРМАЦИЯ .../
    ├── 02-СТРУКТУРА ПРОЕКТА .../
    │   ├── pages/
    │   ├── components/
    │   └── modules/        (если есть модульность)
    │
    ├── 06-БЕЗОПАСНОСТЬ .../ (если есть риск/данные)
    └── 10-АРХИВ .../
```

Если проект маленький — разрешено начать с:
- `README.md` (root),
- `CHANGELOG.md` (root),
- папки `docs/` или `doc/` с файлами: `00🔵`, `01🔵`, `03🔵`.

---

## Полная структура (10 основных разделов) — опционально

Если проект средний/большой, используем базовую “универсальную десятку”:

```
docs/
├── 01-ОБЩАЯ ИНФОРМАЦИЯ .../1
├── 02-СТРУКТУРА ПРОЕКТА .../
├── 03-ФРОНТЕНД .../
├── 04-БЭКЕНД .../
├── 05-ДИЗАЙН .../
├── 06-БЕЗОПАСНОСТЬ .../
├── 07-ФУНКЦИОНАЛ .../
├── 08-РАЗВЕРТЫВАНИЕ .../
├── 09-РУКОВОДСТВА .../
└── 10-АРХИВ .../
```

Правило:
- не создавайте папки “на будущее”, если тема маленькая — достаточно одного файла.

---

## Преимущества системы

1) Универсальность: подходит для web/mobile/desktop/API/библиотек.
2) Практичность: заполняется по мере необходимости.
3) Гибкость: можно добавлять/убирать разделы без поломки структуры.
4) Удобство: русские названия + единые шаблоны.

---

## Примеры применения структуры

### Для web/admin

```
docs/
├── 03-ФРОНТЕНД .../
├── 04-БЭКЕНД .../
└── 06-БЕЗОПАСНОСТЬ .../
```

### Для mobile

```
docs/
├── 03-РАЗРАБОТКА flutter dart архитектура/
├── 04-ДИЗАЙН ui-kit темы адаптивность/
└── 05-ИНТЕГРАЦИИ api push analytics/
```

### Для API сервиса

```
docs/
├── 02-API-ДОКУМЕНТАЦИЯ endpoints методы схемы/
├── 03-АРХИТЕКТУРА слои сервисы база/
└── 05-РАЗВЕРТЫВАНИЕ docker kubernetes мониторинг/
```

---

## Правила именования

### 1) Цифры

Цифры — это сортировка + порядок чтения. Номера обязательны.

### 2) Иконки в имени

- `🔵` — часто используемый документ (открываем регулярно).
- `❌` — документ утверждён/заморожен (менять нельзя без новой ревизии).

Важно:
- `📋/🚧/✅` — только внутри файла.
- В именах файлов/папок не используем другие эмодзи (кроме `🔵` и `❌`).

### 3) Папки

Формат:

```
[NN][❌]-[КЛЮЧЕВЫЕ СЛОВА БОЛЬШИМИ] [детали маленькими]/
```

Пример:
- `02-СТРУКТУРА ПРОЕКТА страницы компоненты/`

### 4) Файлы

Формат:

```
[NN][🔵][❌]-[КЛЮЧЕВОЕ СЛОВО БОЛЬШИМИ] [детали маленькими].md
```

Примеры:
- `01🔵-README.md`
- `03🔵-ROADMAP.md`
- `03❌-ТЕХНОЛОГИИ СТЕК frontend backend devops.md`

### 5) Как выбирать номера

Рекомендованный принцип: сначала “часто используемое”, потом “критичное”, потом “справка”.

Пример для корневых файлов в `docs/` / `doc/`:
- `00🔵` — старт и правила
- `01🔵` — карта доков
- `02🔵` — правила/соглашения (если нужно)
- `03🔵` — roadmap/план
- `05🔵` — план разработки (если отделяете от roadmap)

Правило: файлы одинаковой важности могут иметь одинаковый номер.

---

## Шаблоны документов

### 1) Шаблон “для 95% документов”

```markdown
# [NN]-[НАЗВАНИЕ БОЛЬШИМИ] [детали маленькими]

Статус: 📋 Ожидает / 🚧 В работе / ✅ Готово
Приоритет: 🔥 Критично / 💡 Важно / ⭐ Желательно

---

## История правок

| Дата | Время | Что сделано |
|------|-------|-------------|
| YYYY-MM-DD | HH:mm | ... |

---

## Суть

1–3 предложения: что это, зачем, границы.

---

## Содержание

### 🚧 В работе
- ...

### ⏳ Планируется
- ...

### ✅ Выполнено
~~...~~

---

## Связано
- [Документ](relative/path.md) — зачем
```

### 2) Шаблон страницы (pages/) — опционально

```markdown
# [NN]-[НАЗВАНИЕ СТРАНИЦЫ]

Статус: 📋 Ожидает / 🚧 В работе / ✅ Готово
Приоритет: 🔥 Критично (MVP) / 💡 Важно / ⭐ Желательно

---

## Суть

Зачем эта страница? Какую проблему решает?

---

## Структура

### Layout
- Header
- Sidebar
- Main

### Секции
1) ...
2) ...

---

## Функциональность

### Основное
- [ ] ...

### Edge cases
- ...

---

## Интеграции

- API: ...
- Данные/кэш: ...

---

## Критерии готовности

- [ ] ...
```

### 3) Шаблон API документа — опционально

````markdown
# [NN]-API-ENDPOINTS

Статус: 📋 Ожидает / 🚧 В работе / ✅ Готово

---

## Аутентификация

```http
Authorization: Bearer {token}
```

---

## Endpoints

### GET /api/endpoint

Описание: ...

Request:
```json
{}
```

Response:
```json
{}
```

Ошибки:
- `400` - ...
- `401` - ...
````

---

## Статусы документов (легенда)

| Статус | Значение | Когда использовать |
|--------|----------|-------------------|
| 📋 Ожидает | Шаблон создан | Документ пустой/частично заполнен |
| 🚧 В работе | Заполняется | Активно работаем |
| ✅ Готово | Утверждено | Документ заполнен |
| 🔒 Заморожен | Не редактируется | Архив/старая версия |

---

## Процесс работы (универсальный)

### Этап 1: Создание структуры

- Создать `docs/` или `doc/`
- Положить этот файл + `01🔵-README.md` + `03🔵-ROADMAP.md`
- Создать root `README.md` + `CHANGELOG.md`

### Этап 2: Планирование

- Заполнить roadmap (этапы, приоритеты)
- Создать “скелеты” документов под MVP

### Этап 3: Поэтапное заполнение

- Сначала концепция/структура
- Затем страницы/модули
- Затем безопасность/деплой/эксплуатация

### Этап 4: Релиз

- Обновить roadmap (прогресс)
- В CHANGELOG: `[Unreleased]` -> `[x.y.z] - дата`, создать новый `[Unreleased]`

---

## Чеклист готовности (перед стартом разработки)

- [ ] Есть root `README.md` с командами запуска/проверки
- [ ] Есть root `CHANGELOG.md` с секцией `[Unreleased]`
- [ ] Есть `01🔵-README.md` как карта документации
- [ ] Есть `03🔵-ROADMAP.md` с этапами
- [ ] Определён минимум MVP (страницы/модули/интеграции)

---

## Полезное (PowerShell)

Добавить `❌` в имя файла после номера:

```powershell
$oldName = "03-НАЗВАНИЕ.md"
$newName = "03" + [char]0x274C + "-НАЗВАНИЕ.md"
Rename-Item $oldName $newName
```

---

## Критерии качества (простые)

- Док открывается → за 30 секунд ясно “что это” и “где искать дальше”.
- В каждом документе есть статус и история правок.
- Самые важные/часто используемые документы помечены `🔵`.
- Закрытые решения помечены `❌` и не меняются без ревизии.
